/**
 * PlatformStrip — Standalone platform strip.
 *
 * NOTE: The platform strip is now rendered as part of HeroSection
 * (inside the hero card at the bottom) to match the reference design.
 *
 * This component is kept as a null-render stub to avoid breaking
 * the HomePage import. It may be reactivated for other pages in future.
 */
function PlatformStrip() {
  return null;
}

export default PlatformStrip;
